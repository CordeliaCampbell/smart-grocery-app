import SwiftUI

extension Color {
    /// GitHub accent green — used on active controls, links, and prominent buttons (#3fb950)
    static let githubGreen = Color(red: 0.247, green: 0.725, blue: 0.314)

    /// GitHub canvas default background (#0d1117)
    static let githubCanvas = Color(red: 0.051, green: 0.067, blue: 0.090)

    /// GitHub subtle secondary surface (#161b22)
    static let githubSurface = Color(red: 0.086, green: 0.106, blue: 0.133)

    /// GitHub border default (#30363d)
    static let githubBorder = Color(red: 0.188, green: 0.212, blue: 0.239)

    /// GitHub muted text (#8b949e)
    static let githubMuted = Color(red: 0.545, green: 0.580, blue: 0.620)
}
